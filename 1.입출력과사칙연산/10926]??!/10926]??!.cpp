#include <iostream>
#include <string.h>
using namespace std;
// trigraph problem : ??!
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    string name = "";
    cin >> name;
    cout << name << "\?\?!" <<endl;
    return 0;
}
